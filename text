agentic-bi-platform/
├── graph/
│   ├── __init__.py
│   ├── state.py              ← AgentState definition
│   └── bi_agent_graph.py     ← LangGraph StateGraph wiring
├── agents/
│   ├── __init__.py
│   ├── monitor_agent.py      ← stub
│   ├── diagnose_agent.py     ← stub
│   └── heal_agent.py         ← stub
├── config.py                 ← Gemini API config
├── requirements.txt
└── main.py                   ← entry point


Full Agent Graph Flow

                    ┌─────────────┐
                    │   Monitor   │  ← Polls Power BI REST API
                    └──────┬──────┘
                           │
              ┌────────────▼────────────┐
              │   should_diagnose()?    │
              └────────┬────────────────┘
                       │
          ┌────────────┴──────────────┐
          │ healthy                   │ failed/degraded
          ▼                           ▼
         END               ┌─────────────────┐
                           │    Diagnose     │  ← Gemini 2.5 Flash
                           └────────┬────────┘
                                    │
                           ┌────────▼────────┐
                           │     Heal        │  ← dbt Core CLI
                           └────────┬────────┘
                                    │
                           ┌────────▼────────┐
                           │     Notify      │  ← Gmail SMTP
                           └────────┬────────┘
                                    │
                                   END