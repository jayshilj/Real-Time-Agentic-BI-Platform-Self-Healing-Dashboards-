Real-Time-Agentic-BI-Platform/
├── api/
│   ├── __init__.py
│   ├── main.py                  ← FastAPI app
│   └── routers/
│       ├── __init__.py
│       ├── health.py            ← GET /api/v1/health
│       └── dashboards.py        ← GET/POST /api/v1/dashboards
├── agents/
│   ├── monitor_agent.py
│   ├── diagnose_agent.py
│   ├── heal_agent.py
│   └── notify_agent.py
├── graph/
│   ├── bi_agent_graph.py
│   └── state.py
├── tools/
│   ├── powerbi_tools.py
│   ├── gemini_tools.py
│   └── email_tools.py
├── config.py
├── main.py                      ← CLI runner (keep for testing)
└── requirements.txt


Full Agent Graph Flow

                    ┌─────────────┐
                    │   Monitor   │  ← Polls Power BI REST API
                    └──────┬──────┘
                           │
              ┌────────────▼────────────┐
              │   should_diagnose()?    │
              └────────┬────────────────┘
                       │
          ┌────────────┴──────────────┐
          │ healthy                   │ failed/degraded
          ▼                           ▼
         END               ┌─────────────────┐
                           │    Diagnose     │  ← Gemini 2.5 Flash
                           └────────┬────────┘
                                    │
                           ┌────────▼────────┐
                           │     Heal        │  ← dbt Core CLI
                           └────────┬────────┘
                                    │
                           ┌────────▼────────┐
                           │     Notify      │  ← Gmail SMTP
                           └────────┬────────┘
                                    │
                                   END